/*
 This set of controls is for controlling the appearance on a threeJS scene run as the background of a html page.
 Currently it does nothing and just acts as a mock of OrbitControls.js
*/

THREE.OrbitControls = function ( object, domElement ) {
    return
}

THREE.OrbitControls.prototype = Object.create( THREE.EventDispatcher.prototype );